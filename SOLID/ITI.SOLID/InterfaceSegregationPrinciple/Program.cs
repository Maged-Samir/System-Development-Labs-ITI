namespace InterfaceSegregationPrinciple
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /// <summary>
            /// The ISP states that clients should not be forced to depend on interfaces they do not use. 
            /// It means that you should design small, cohesive interfaces that are specific to the needs
            /// of clients, rather than creating large, monolithic interfaces. This helps avoid unnecessary
            /// dependencies and minimizes the impact of changes.
            /// </summary>
        }
    }
}