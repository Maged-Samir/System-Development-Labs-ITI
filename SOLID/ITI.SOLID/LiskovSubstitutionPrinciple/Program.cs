namespace LiskovSubstitutionPrinciple
{
    public class Program
    {
        static void Main(string[] args)
        {
            ///<summary>
            /// The LSP states that objects of a superclass should be replaceable with objects of its 
            /// subclasses without affecting the correctness of the program. In other words, if you have a
            /// base class and you create derived classes from it, you should be able to use those 
            /// derived classes interchangeably with the base class without breaking the functionality or
            /// assumptions of the program.
            ///</summary>
        }
    }
}