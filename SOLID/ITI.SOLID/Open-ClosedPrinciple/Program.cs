namespace Open_ClosedPrinciple
{
    public class Program
    {
        static void Main(string[] args)
        {
            /// <summary>
            /// The OCP states that software entities (classes, modules, functions, etc.) should be open 
            /// for extension but closed for modification. In other words, you should be able to extend the 
            /// behavior of a system without modifying its existing code. This can be achieved by using 
            /// techniques such as inheritance, interfaces, and abstractions.
            /// </summary>
        }
    }
}